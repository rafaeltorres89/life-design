import React from 'react';

import Navbar from '../navbar';

const Header = () => {
    return (
        <header>
            <h1>Header do site</h1>
            <Navbar />
        </header>
    );
}

export default Header;