import React from 'react';

import Typography from '../../components/typography';

const Aside = () => {
    return (
        <main>
            <Typography />
        </main>
    );
}

export default Aside;